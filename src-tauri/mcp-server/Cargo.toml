[package]
name = "inkwell-mcp"
version = "0.1.0"
edition = "2021"
description = "MCP server for inkwell — exposes vault notes to Claude"

[[bin]]
name = "inkwell-mcp"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
